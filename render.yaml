services:
  - type: web
    name: crypto-trader-analyst
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

databases:
  - name: trading-cache
    plan: free
    region: ohio
